import React from 'react';

import useFetch from 'hooks/useFetch';
import { HttpMethod } from 'enums';

function LoginPage() {
  return <div>Hi, this is the login page</div>;
}

export default LoginPage;
